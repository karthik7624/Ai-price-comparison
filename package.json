{
  "name": "price-comparison-website",
  "version": "1.0.0",
  "description": "A comprehensive price comparison website that compares products across multiple e-commerce platforms",
  "main": "server/index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server:dev\" \"npm run client\"",
    "server:dev": "cd server && nodemon index.js",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["price-comparison", "e-commerce", "web-scraping", "react", "nodejs"],
  "author": "Price Comparison Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}